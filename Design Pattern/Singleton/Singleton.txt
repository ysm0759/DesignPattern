
생성

싱글톤 패턴 (Singleton)


Instacne() 연산을 정의하여 유일한 인스턴스로 접근할 수 있도록 한다.
유일한 인스턴스를 생성하는 책임을 맡습니다.



하나의 클래스 안에서 하나의 객체만이 생성되도록 보장해주는 패턴
다른 클래스에서 접근은 가능할 수 있지만 생성은 하지못함.




1. 유일하게 존재하는 인스턴스로의 접근을 통제한다.
클래스 자체가 인스턴스를 캡슐화하기 떄문에 이 클래스에서 사용자가 언제 어떻게 이 인스턴스에
접근할 수 있는지 제어한다.

2. 이름 공간을 좁힌다.
단일체 패턴은 전역 변수보다 더 좋다. 전역 변수를 사용해서 이름 공간을 망치는 일을 없애주기 떄문이다.
전역 변수를 정의하여 발생하는 디버깅의 어려움 등 문제를 없앤다.

3. 연산 및 표현의 정제를 허용한다.
싱글톤 클래스는 상속될 수 있기 때문에 이 상속된 서브클래스를 통해서 새로운 인스턴스를 만들 수 있다.

4. 인스턴스의 개수를 변경하기가 자유롭다.
응용프로그램이 사용하는 인스턴스가 다수여야 할 때도 똑같은 방법을 사용할 수 있다.
클래스의 인스턴스에 접근할 수 있는 허용범위를 결정하는 연산만 변경하면 된다.

5. 클래스 연산을 사용하는 것보다 훨씬 유연한 방법이다.
클래스의 인스턴스가 하나 이상 존재할 수 있도록 설계를 변경하는 것은 어렵다.
서브 클래스들이 이 연산을 오버라이드 할 수 없다.


구현
1. 인스턴스가 유일해야 함을 보장한다.

2. 클래스를 서브클래싱한다.




https://drive.google.com/file/d/1FCQJSD3WoRdjmseKu64a2hQfZx0KtVrv/view?usp=sharing
클래스 다이어그램


https://www.dofactory.com/net/singleton-design-pattern
Singleton 패턴 구현

